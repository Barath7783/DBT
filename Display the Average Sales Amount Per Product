This query uses GROUP BY to aggregate sales by Product_ID and AVG to calculate the average sales amount for each product.

SELECT
    Product_ID,
    AVG(Amount) AS Average_Sales_Amount
FROM
    Sales
GROUP BY
    Product_ID
ORDER BY
    Product_ID;
